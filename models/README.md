This directory should contain your trained model and encoder.

Add the following files here before running the app in production:

- models/model.pkl
- models/encoder.pkl

If you do not have these, the app will run in demo mode with synthetic predictions.
